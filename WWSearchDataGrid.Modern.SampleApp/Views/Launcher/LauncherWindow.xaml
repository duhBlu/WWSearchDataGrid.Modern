<Window x:Class="WWSearchDataGrid.Modern.SampleApp.Views.Launcher.LauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="WWSearchDataGrid — Sample Launcher"
        Style="{DynamicResource SampleAppWindowStyle}"
        Width="780"
        Height="560"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d">
    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="24,20,24,12">
            <TextBlock Text="WWSearchDataGrid Samples"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="#1C1C1C" />
            <TextBlock Text="Pick a focused sample to exercise a specific feature or data shape."
                       FontSize="13"
                       Foreground="#666"
                       Margin="0,4,0,0" />
        </StackPanel>

        <ListBox x:Name="SampleList"
                 Grid.Row="1"
                 Margin="16,0,16,0"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 HorizontalContentAlignment="Stretch"
                 MouseDoubleClick="OnSampleDoubleClick"
                 KeyDown="OnSampleListKeyDown">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,8" />
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Card"
                                        Background="White"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="16,12">
                                    <ContentPresenter />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Card" Property="BorderBrush" Value="#0078D4" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Card" Property="BorderBrush" Value="#0078D4" />
                                        <Setter TargetName="Card" Property="Background" Value="#F3F8FD" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Foreground="#1C1C1C" />
                        <TextBlock Text="{Binding Description}"
                                   FontSize="12"
                                   Foreground="#555"
                                   TextWrapping="Wrap"
                                   Margin="0,4,0,8" />
                        <ItemsControl ItemsSource="{Binding Tags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#EEF4FA"
                                            BorderBrush="#CFE3F4"
                                            BorderThickness="1"
                                            CornerRadius="3"
                                            Padding="6,2"
                                            Margin="0,0,4,0">
                                        <TextBlock Text="{Binding}"
                                                   FontSize="11"
                                                   Foreground="#0857A0" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="24,12,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="Double-click or press Enter to open."
                       FontSize="12"
                       Foreground="#888"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Content="Open"
                    Padding="20,6"
                    IsDefault="True"
                    Click="OnOpenClick" />
        </Grid>
    </Grid>
</Window>
