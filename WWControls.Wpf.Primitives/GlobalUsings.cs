// Standalone primitive controls — depend only on Core (and the WPF framework). This global using
// lets the primitives reference their siblings + the status enums without per-file usings.
global using WWControls.Wpf.Primitives;
