<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:WWControls.Wpf.Converters;assembly=WWControls.Wpf.Grid"
                    xmlns:behaviors="clr-namespace:WWControls.Wpf.Behaviors;assembly=WWControls.Wpf.Grid"
                    xmlns:sdg="http://schemas.wwcontrols.com/wpf">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/WWControls.Wpf.Themes.Default;component/Resources/Icons/Icons.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Converters  -->
    <converters:StringEqualityConverter x:Key="StringEqualityConverter" />
    <converters:BooleanToVisibilityCollapsedConverter x:Key="BooleanToVisibilityCollapsedConverter" />

    <!--#region Filter Tokens-->

    <!--  Open Bracket Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensOpenBracket}">
        <Border Background="Transparent"
                BorderThickness="1"
                Margin="0,0,-8,0"
                Width="10"
                Height="24"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                Tag="{Binding FilterId}">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#9a9a9a" Offset="0" />
                    <GradientStop Color="#9a9a9a" Offset="0.5" />
                    <GradientStop Color="Transparent" Offset="0.51" />
                    <GradientStop Color="Transparent" Offset="1" />
                </LinearGradientBrush>
            </Border.BorderBrush>
        </Border>
    </DataTemplate>

    <!--  Column Name Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensColumnName}">
        <Border Background="#ffc69b"
                Margin="1,0"
                Padding="4,0"
                Height="20"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                CornerRadius="4"
                Tag="{Binding FilterId}">
            <TextBlock Foreground="Black"
                       Text="{Binding DisplayText}"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold" />
        </Border>
    </DataTemplate>

    <!--  Search Type Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensSearchType}">
        <Border Background="Transparent"
                Margin="2,1"
                Padding="4,3"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                Tag="{Binding FilterId}">
            <TextBlock Foreground="Black"
                       Text="{Binding DisplayText}"
                       VerticalAlignment="Center"
                       FontSize="12" />
        </Border>
    </DataTemplate>

    <!--  Unary Search Type Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensUnarySearchType}">
        <Grid x:Name="TokenRoot"
              Cursor="Hand"
              Height="22"
              behaviors:TokenHoverBehavior.IsEnabled="True"
              behaviors:TokenConfirmationBehavior.IsEnabled="True"
              Tag="{Binding FilterId}">
            <Border x:Name="TokenBorder"
                    Background="#bbe2c5"
                    Margin="1"
                    Padding="4,2"
                    CornerRadius="4">
                <TextBlock Foreground="Black"
                           Text="{Binding DisplayText}"
                           VerticalAlignment="Center" />
            </Border>

            <!--  Confirmation overlay  -->
            <Button BorderThickness="0"
                    Visibility="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=(behaviors:TokenConfirmationBehavior.IsInConfirmationState), Converter={StaticResource BooleanToVisibilityCollapsedConverter}}"
                    Cursor="Hand"
                    Command="{Binding RemoveValueFromTokenCommand, RelativeSource={RelativeSource AncestorType=sdg:FilterSummaryPanel}}"
                    CommandParameter="{Binding}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Tag="{Binding FilterId}">
                <sdg:Icon Width="12"
                             Height="12"
                             IconSource="{StaticResource {x:Static sdg:IconKeys.IconClear}}"
                             IconBrush="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
                <Button.ToolTip>
                    <ToolTip>
                        <ToolTip.ContentTemplate>
                            <DataTemplate>
                                <TextBlock VerticalAlignment="Center">
                                    <Run Text="Remove '" />
                                    <Run Text="{Binding DisplayText}" />
                                    <Run Text="' Search Template" />
                                </TextBlock>
                            </DataTemplate>
                        </ToolTip.ContentTemplate>
                    </ToolTip>
                </Button.ToolTip>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#61000000" />
                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <ContentPresenter x:Name="contentPresenter"
                                                          Focusable="False"
                                                          Margin="{TemplateBinding Padding}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          RecognizesAccessKey="True"
                                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#7A000000" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#8F000000" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </DataTemplate>

    <!--  Value Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensValue}">
        <Grid x:Name="TokenRoot"
              Background="Transparent"
              Cursor="Hand"
              Height="22"
              behaviors:TokenHoverBehavior.IsEnabled="True"
              behaviors:TokenConfirmationBehavior.IsEnabled="True"
              Tag="{Binding FilterId}">
            <Border Background="#bbe2c5"
                    Margin="1"
                    Padding="4,0"
                    CornerRadius="4">
                <TextBlock Foreground="Black"
                           Text="{Binding DisplayText}"
                           VerticalAlignment="Center" />
            </Border>

            <!--  Confirmation overlay  -->
            <Button BorderThickness="0"
                    Visibility="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=(behaviors:TokenConfirmationBehavior.IsInConfirmationState), Converter={StaticResource BooleanToVisibilityCollapsedConverter}}"
                    Cursor="Hand"
                    Command="{Binding RemoveValueFromTokenCommand, RelativeSource={RelativeSource AncestorType=sdg:FilterSummaryPanel}}"
                    CommandParameter="{Binding}"
                    Margin="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Tag="{Binding FilterId}">
                <sdg:Icon Width="12"
                             Height="12"
                             IconSource="{StaticResource {x:Static sdg:IconKeys.IconClear}}"
                             IconBrush="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
                <Button.ToolTip>
                    <ToolTip>
                        <ToolTip.ContentTemplate>
                            <DataTemplate>
                                <TextBlock VerticalAlignment="Center">
                                    <Run Text="Remove '" />
                                    <Run Text="{Binding DisplayText}" />
                                    <Run Text="' Search Template" />
                                </TextBlock>
                            </DataTemplate>
                        </ToolTip.ContentTemplate>
                    </ToolTip>
                </Button.ToolTip>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#61000000" />
                        <Setter Property="BorderBrush" Value="#DC3656" />
                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <ContentPresenter x:Name="contentPresenter"
                                                          Focusable="False"
                                                          Margin="{TemplateBinding Padding}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                          RecognizesAccessKey="True"
                                                          TextElement.Foreground="{TemplateBinding Foreground}" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#7A000000" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#8F000000" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </DataTemplate>

    <!--  Value Operator Token Template (between values "and")  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensValueOperator}">
        <Border Background="Transparent"
                Margin="1,1"
                Padding="3,3"
                Height="24"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                Tag="{Binding FilterId}">
            <TextBlock Foreground="Black"
                       Text="{Binding DisplayText}"
                       VerticalAlignment="Center"
                       FontSize="11" />
        </Border>
    </DataTemplate>

    <!--  Closing Bracket Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensCloseBracket}">
        <Border Background="Transparent"
                BorderThickness="1"
                Margin="-8,0,0,0"
                Width="10"
                Height="26"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                Tag="{Binding FilterId}">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="1,0" EndPoint="0,0">
                    <GradientStop Color="#9a9a9a" Offset="0" />
                    <GradientStop Color="#9a9a9a" Offset="0.5" />
                    <GradientStop Color="Transparent" Offset="0.51" />
                    <GradientStop Color="Transparent" Offset="1" />
                </LinearGradientBrush>
            </Border.BorderBrush>
        </Border>
    </DataTemplate>

    <!--  Search Template Group logical Connector Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensGroupLogicalConnector}">
        <Border x:Name="OperatorBorder"
                Background="#cee8fb"
                Cursor="Hand"
                Margin="4,2,14,2"
                Padding="4,0"
                Height="22"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                CornerRadius="6"
                ToolTip="Click to toggle between AND/OR"
                Tag="{Binding FilterId}">
            <Border.InputBindings>
                <MouseBinding Command="{Binding ToggleOperatorCommand, RelativeSource={RelativeSource AncestorType=sdg:FilterSummaryPanel}}"
                              CommandParameter="{Binding}"
                              MouseAction="LeftClick" />
            </Border.InputBindings>
            <TextBlock Foreground="#0078D4"
                       Text="{Binding DisplayText}"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       FontSize="11" />
        </Border>
        <DataTemplate.Triggers>
            <Trigger SourceName="OperatorBorder" Property="IsMouseOver" Value="True">
                <Setter TargetName="OperatorBorder" Property="Background" Value="#b8d8f0" />
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!--  Search Template Logical Connector Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensTemplateLogicalConnector}">
        <Border x:Name="TemplateOperatorBorder"
                Background="#cee8fb"
                Cursor="Hand"
                Margin="2,2,2,2"
                Padding="4,0"
                Height="20"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                CornerRadius="4"
                ToolTip="Click to toggle between AND/OR"
                Tag="{Binding FilterId}">
            <Border.InputBindings>
                <MouseBinding Command="{Binding ToggleOperatorCommand, RelativeSource={RelativeSource AncestorType=sdg:FilterSummaryPanel}}"
                              CommandParameter="{Binding}"
                              MouseAction="LeftClick" />
            </Border.InputBindings>
            <TextBlock Foreground="#0078D4"
                       Text="{Binding DisplayText}"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       FontSize="10" />
        </Border>
        <DataTemplate.Triggers>
            <Trigger SourceName="TemplateOperatorBorder" Property="IsMouseOver" Value="True">
                <Setter TargetName="TemplateOperatorBorder" Property="Background" Value="#b8d8f0" />
            </Trigger>
        </DataTemplate.Triggers>
    </DataTemplate>

    <!--  Remove Action Token Template  -->
    <DataTemplate x:Key="{x:Static sdg:GridThemeKeys.FilterTokensRemoveAction}">
        <Button Cursor="Hand"
                Command="{Binding RemoveTokenFilterCommand, RelativeSource={RelativeSource AncestorType=sdg:FilterSummaryPanel}}"
                CommandParameter="{Binding}"
                Margin="1,0,-2,0"
                BorderThickness="0"
                VerticalAlignment="Top"
                Padding="3"
                behaviors:TokenHoverBehavior.IsEnabled="True"
                ToolTip="Remove Filter"
                Tag="{Binding FilterId}">
            <sdg:Icon Width="6"
                         Height="6"
                         StrokeThickness="3"
                         IconSource="{StaticResource {x:Static sdg:IconKeys.IconClear}}"
                         IconBrush="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="Background" Value="DarkGray" />
                    <Setter Property="Opacity" Value="0" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type Button}">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="12">
                                    <ContentPresenter x:Name="contentPresenter"
                                                      Focusable="False"
                                                      Margin="{TemplateBinding Padding}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                      RecognizesAccessKey="True"
                                                      TextElement.Foreground="{TemplateBinding Foreground}" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <!--  Show when this token's FilterId matches HoveredFilterId  -->
                        <DataTrigger Value="True">
                            <DataTrigger.Binding>
                                <MultiBinding Converter="{StaticResource StringEqualityConverter}">
                                    <Binding Path="FilterId" />
                                    <Binding RelativeSource="{RelativeSource AncestorType=sdg:FilterSummaryPanel}" Path="HoveredFilterId" />
                                </MultiBinding>
                            </DataTrigger.Binding>
                            <Setter Property="Opacity" Value="1" />
                        </DataTrigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="1" />
                            <Setter Property="Background" Value="Gray" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="Black" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </DataTemplate>

    <!--#endregion-->

    <!--  Token Template Selector  -->
    <sdg:FilterTokenTemplateSelector x:Key="GenericFilterTokenTemplateSelector"
                                     OpenBracketTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensOpenBracket}}"
                                     ColumnNameTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensColumnName}}"
                                     SearchTypeTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensSearchType}}"
                                     UnarySearchTypeTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensUnarySearchType}}"
                                     ValueTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensValue}}"
                                     ValueOperatorTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensValueOperator}}"
                                     CloseBracketTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensCloseBracket}}"
                                     GroupLogicalConnectorTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensGroupLogicalConnector}}"
                                     TemplateLogicalConnectorTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensTemplateLogicalConnector}}"
                                     RemoveActionTemplate="{StaticResource {x:Static sdg:GridThemeKeys.FilterTokensRemoveAction}}" />

</ResourceDictionary>