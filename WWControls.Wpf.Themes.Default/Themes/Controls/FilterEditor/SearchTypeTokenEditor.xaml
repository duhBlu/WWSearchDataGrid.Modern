<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sdg="http://schemas.wwcontrols.com/wpf"
                    xmlns:converters="clr-namespace:WWControls.Wpf.Converters;assembly=WWControls.Wpf.Grid">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/WWControls.Wpf.Themes.Default;component/Themes/Controls/Primitives/DefaultControls.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converters:SearchTypeToIconConverter x:Key="FilterEditorSearchTypeToIconConverter" />
    <converters:EnumToStringConverter x:Key="FilterEditorEnumToStringConverter" />

    <Style x:Key="{x:Static sdg:ThemeKeys.FilterEditorSearchTypeToken}" TargetType="{x:Type sdg:SearchTypeTokenEditor}">
        <Setter Property="Focusable" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sdg:SearchTypeTokenEditor}">
                    <Grid>
                        <ToggleButton x:Name="PART_Toggle"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      Focusable="True"
                                      Cursor="Hand"
                                      Padding="0">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="ChipBorder"
                                            Background="#bcd7e7"
                                            Margin="1,0"
                                            Padding="6,2"
                                            MinHeight="22"
                                            CornerRadius="4">
                                        <TextBlock Foreground="Black"
                                                   Text="{TemplateBinding Content}"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <ToggleButton.Content>
                                <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="DisplayText" />
                            </ToggleButton.Content>
                        </ToggleButton>

                        <Popup x:Name="PART_Popup"
                               AllowsTransparency="True"
                               IsOpen="{Binding IsChecked, ElementName=PART_Toggle, Mode=TwoWay}"
                               StaysOpen="False"
                               PopupAnimation="Fade"
                               Placement="Bottom"
                               PlacementTarget="{Binding ElementName=PART_Toggle}">
                            <Border Background="White"
                                    BorderBrush="#BBB"
                                    BorderThickness="1"
                                    Padding="2"
                                    CornerRadius="3"
                                    SnapsToDevicePixels="True">
                                <Border.Effect>
                                    <DropShadowEffect Color="#22000000"
                                                      BlurRadius="6"
                                                      ShadowDepth="2" />
                                </Border.Effect>
                                <ListBox x:Name="PART_List"
                                         BorderThickness="0"
                                         MinWidth="180"
                                         MaxHeight="300"
                                         ItemsSource="{Binding ValidSearchTypes, RelativeSource={RelativeSource TemplatedParent}}"
                                         SelectedItem="{Binding SelectedSearchType, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <ListBox.ItemContainerStyle>
                                        <Style BasedOn="{StaticResource {x:Static sdg:ThemeKeys.PrimitivesListBoxItem}}" TargetType="ListBoxItem" />
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <sdg:Icon IconSource="{Binding Converter={StaticResource FilterEditorSearchTypeToIconConverter}}"
                                                          Margin="0,0,6,0"
                                                          VerticalAlignment="Center"
                                                          Width="14"
                                                          Height="14" />
                                                <TextBlock Text="{Binding Converter={StaticResource FilterEditorEnumToStringConverter}}" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource {x:Static sdg:ThemeKeys.FilterEditorSearchTypeToken}}" TargetType="{x:Type sdg:SearchTypeTokenEditor}" />

</ResourceDictionary>
