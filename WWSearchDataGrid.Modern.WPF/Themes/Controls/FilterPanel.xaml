<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converters="clr-namespace:WWSearchDataGrid.Modern.WPF.Converters"
                    xmlns:sdg="clr-namespace:WWSearchDataGrid.Modern.WPF">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/WWSearchDataGrid.Modern.WPF;component/Themes/Common/CommonStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  Converters  -->
    <converters:BooleanToVisibilityCollapsedConverter x:Key="BooleanToVisibilityConverter" />
    <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter" />

    <Color x:Key="PrimaryColor">#0078D4</Color>
    <SolidColorBrush x:Key="SelectionBrush" Color="{StaticResource PrimaryColor}" />

    <!--  Filter Chip Style  -->
    <Style x:Key="FilterChipStyle" TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="DimGray" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Margin" Value="0,0,4,4" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#BBDEFB" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Remove Button Style  -->

    <!--  Action Button Style  -->
    <Style x:Key="FilterPanelActionButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#F5F5F5" />
        <Setter Property="BorderBrush" Value="#CCC" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Margin" Value="4,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="4">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#E0E0E0" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#D0D0D0" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#F9F9F9" />
                            <Setter Property="Foreground" Value="#999" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Default FilterPanel Style  -->
    <Style x:Key="Default_FilterPanel_Style" TargetType="{x:Type sdg:FilterPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sdg:FilterPanel}">
                    <Border Background="#F8F9FA"
                            BorderBrush="#E1E5E9"
                            BorderThickness="0,1,0,0"
                            Visibility="{Binding HasActiveFilters, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Padding="12,8">

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <!--  Filters enabled checkbox  -->
                                <ColumnDefinition Width="*" />
                                <!--  Filter chips  -->
                                <ColumnDefinition Width="Auto" />
                                <!--  Action buttons  -->
                            </Grid.ColumnDefinitions>

                            <!--  Filters Enabled Checkbox  -->
                            <CheckBox x:Name="PART_FiltersEnabledCheckBox"
                                      Style="{StaticResource Default_CheckBox_Style}"
                                      Grid.Column="0"
                                      IsChecked="{Binding FiltersEnabled, RelativeSource={RelativeSource TemplatedParent}}"
                                      Margin="0,0,8,0"
                                      VerticalAlignment="Center"
                                      Content="Filters Active"
                                      ToolTip="Enable or disable all filters without clearing filter definitions" />

                            <!--  Filter Chips Area  -->
                            <ScrollViewer Grid.Column="1"
                                          VerticalAlignment="Center"
                                          HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Disabled">
                                <WrapPanel Orientation="Horizontal" ItemHeight="32">
                                    <ItemsControl x:Name="PART_FiltersItemsControl" ItemsSource="{Binding ActiveFilters, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <!--  Conjunction text (AND) - shown before all items except the first  -->
                                                    <TextBlock x:Name="ConjunctionText"
                                                               Foreground="#333"
                                                               Text="{Binding Conjunction}"
                                                               Visibility="Visible"
                                                               Margin="0,0,12,0"
                                                               VerticalAlignment="Center"
                                                               FontWeight="Bold"
                                                               FontSize="12" />

                                                    <!--  Filter chip  -->
                                                    <Grid Background="Transparent">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <Border Grid.Column="0"
                                                                Background="Transparent"
                                                                BorderBrush="DimGray"
                                                                BorderThickness="1"
                                                                Padding="2"
                                                                CornerRadius="3">
                                                            <StackPanel Orientation="Horizontal" Margin="2">

                                                                <!--  Column Name Component  -->
                                                                <Border Background="#ffc69b"
                                                                        Margin="0,0,2,0"
                                                                        Padding="4,2"
                                                                        CornerRadius="2">
                                                                    <TextBlock Foreground="Black"
                                                                               Text="{Binding ColumnName}"
                                                                               VerticalAlignment="Center"
                                                                               FontWeight="SemiBold" />
                                                                </Border>

                                                                <!--  Filter Components Collection with Fallback  -->
                                                                <Grid>
                                                                    <!--  Use ItemsControl for multiple components  -->
                                                                    <ItemsControl ItemsSource="{Binding FilterComponents}">
                                                                        <ItemsControl.Style>
                                                                            <Style TargetType="ItemsControl">
                                                                                <Setter Property="Visibility" Value="Visible" />
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding FilterComponents.Count}" Value="0">
                                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </ItemsControl.Style>
                                                                        <ItemsControl.ItemsPanel>
                                                                            <ItemsPanelTemplate>
                                                                                <StackPanel Orientation="Horizontal" />
                                                                            </ItemsPanelTemplate>
                                                                        </ItemsControl.ItemsPanel>
                                                                        <ItemsControl.ItemTemplate>
                                                                            <DataTemplate>
                                                                                <StackPanel Orientation="Horizontal">

                                                                                    <!--  Conjunction (AND/OR) for multiple conditions  -->
                                                                                    <Border Background="Transparent"
                                                                                            Visibility="{Binding Conjunction, Converter={StaticResource StringToVisibilityConverter}}"
                                                                                            Margin="2,0,2,0"
                                                                                            Padding="4,2"
                                                                                            VerticalAlignment="Center"
                                                                                            CornerRadius="2">
                                                                                        <TextBlock Text="{Binding Conjunction}"
                                                                                                   VerticalAlignment="Center"
                                                                                                   FontWeight="Bold"
                                                                                                   FontSize="10" />
                                                                                    </Border>

                                                                                    <!--  Search Type Component  -->
                                                                                    <Border Margin="0,0,2,0"
                                                                                            Padding="4,2"
                                                                                            CornerRadius="2">
                                                                                        <Border.Style>
                                                                                            <Style TargetType="Border">
                                                                                                <Setter Property="Background" Value="Transparent" />
                                                                                                <Style.Triggers>
                                                                                                    <DataTrigger Binding="{Binding HasNoInputValues}" Value="True">
                                                                                                        <Setter Property="Background" Value="#bcd7e7" />
                                                                                                    </DataTrigger>
                                                                                                </Style.Triggers>
                                                                                            </Style>
                                                                                        </Border.Style>
                                                                                        <TextBlock Text="{Binding SearchTypeText}"
                                                                                                   VerticalAlignment="Center"
                                                                                                   FontSize="12" />
                                                                                    </Border>

                                                                                    <!--  Value Components - Multiple Values or Single Value  -->
                                                                                    <Grid>
                                                                                        <!--  Multiple Value Items  -->
                                                                                        <ItemsControl ItemsSource="{Binding ValueItems}">
                                                                                            <ItemsControl.Style>
                                                                                                <Style TargetType="ItemsControl">
                                                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                                                    <Style.Triggers>
                                                                                                        <DataTrigger Binding="{Binding HasMultipleValues}" Value="True">
                                                                                                            <Setter Property="Visibility" Value="Visible" />
                                                                                                        </DataTrigger>
                                                                                                        <DataTrigger Binding="{Binding HasNoInputValues}" Value="True">
                                                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                                                        </DataTrigger>
                                                                                                    </Style.Triggers>
                                                                                                </Style>
                                                                                            </ItemsControl.Style>
                                                                                            <ItemsControl.ItemsPanel>
                                                                                                <ItemsPanelTemplate>
                                                                                                    <StackPanel Orientation="Horizontal" />
                                                                                                </ItemsPanelTemplate>
                                                                                            </ItemsControl.ItemsPanel>
                                                                                            <ItemsControl.ItemTemplate>
                                                                                                <DataTemplate>
                                                                                                    <Border Margin="0,0,2,0"
                                                                                                            Padding="4,2"
                                                                                                            CornerRadius="2">
                                                                                                        <Border.Style>
                                                                                                            <Style TargetType="Border">
                                                                                                                <Setter Property="Background" Value="#bcd7e7" />
                                                                                                            </Style>
                                                                                                        </Border.Style>
                                                                                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                                                                                    </Border>
                                                                                                </DataTemplate>
                                                                                            </ItemsControl.ItemTemplate>
                                                                                        </ItemsControl>

                                                                                        <!--  Single Primary Value  -->
                                                                                        <Border Margin="0,0,2,0"
                                                                                                Padding="4,2"
                                                                                                CornerRadius="2">
                                                                                            <Border.Style>
                                                                                                <Style TargetType="Border">
                                                                                                    <Setter Property="Background" Value="#bbe2c5" />
                                                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                                                    <Style.Triggers>
                                                                                                        <DataTrigger Binding="{Binding IsDateInterval}" Value="True">
                                                                                                            <Setter Property="Background" Value="#bcd7e7" />
                                                                                                        </DataTrigger>
                                                                                                        <DataTrigger Binding="{Binding HasNoInputValues}" Value="True">
                                                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                                                        </DataTrigger>
                                                                                                        <DataTrigger Binding="{Binding HasMultipleValues}" Value="True">
                                                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                                                        </DataTrigger>
                                                                                                    </Style.Triggers>
                                                                                                </Style>
                                                                                            </Border.Style>
                                                                                            <TextBlock Text="{Binding PrimaryValue}" VerticalAlignment="Center" />
                                                                                        </Border>
                                                                                    </Grid>

                                                                                    <!--  Value Operator (e.g., "and" for Between operations)  -->
                                                                                    <Border Background="Transparent"
                                                                                            Visibility="{Binding ValueOperatorText, Converter={StaticResource StringToVisibilityConverter}}"
                                                                                            Margin="0,0,2,0"
                                                                                            Padding="4,2"
                                                                                            CornerRadius="2">
                                                                                        <TextBlock Text="{Binding ValueOperatorText}" VerticalAlignment="Center" />
                                                                                    </Border>

                                                                                    <!--  Secondary Value Component  -->
                                                                                    <Border Visibility="{Binding SecondaryValue, Converter={StaticResource StringToVisibilityConverter}}"
                                                                                            Margin="0,0,2,0"
                                                                                            Padding="4,2"
                                                                                            CornerRadius="2">
                                                                                        <Border.Style>
                                                                                            <Style TargetType="Border">
                                                                                                <Setter Property="Background" Value="#bbe2c5" />
                                                                                                <Style.Triggers>
                                                                                                    <DataTrigger Binding="{Binding IsDateInterval}" Value="True">
                                                                                                        <Setter Property="Background" Value="#bcd7e7" />
                                                                                                    </DataTrigger>
                                                                                                </Style.Triggers>
                                                                                            </Style>
                                                                                        </Border.Style>
                                                                                        <TextBlock Text="{Binding SecondaryValue}" VerticalAlignment="Center" />
                                                                                    </Border>

                                                                                </StackPanel>
                                                                            </DataTemplate>
                                                                        </ItemsControl.ItemTemplate>
                                                                    </ItemsControl>

                                                                    <!--  Fallback to legacy display when FilterComponents is empty  -->
                                                                    <StackPanel Orientation="Horizontal">
                                                                        <StackPanel.Style>
                                                                            <Style TargetType="StackPanel">
                                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding FilterComponents.Count}" Value="0">
                                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </StackPanel.Style>

                                                                        <!--  Fallback Search Type Component  -->
                                                                        <Border Margin="0,0,2,0"
                                                                                Padding="4,2"
                                                                                CornerRadius="2">
                                                                            <Border.Style>
                                                                                <Style TargetType="Border">
                                                                                    <Setter Property="Background" Value="Transparent" />
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding HasNoInputValues}" Value="True">
                                                                                            <Setter Property="Background" Value="LightGreen" />
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </Border.Style>
                                                                            <TextBlock Text="{Binding SearchTypeText}" VerticalAlignment="Center" />
                                                                        </Border>

                                                                        <!--  Fallback Value Components  -->
                                                                        <Grid>
                                                                            <!--  Fallback Multiple Value Items  -->
                                                                            <ItemsControl ItemsSource="{Binding FilterComponents[0].ValueItems}">
                                                                                <ItemsControl.Style>
                                                                                    <Style TargetType="ItemsControl">
                                                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                                                        <Style.Triggers>
                                                                                            <MultiDataTrigger>
                                                                                                <MultiDataTrigger.Conditions>
                                                                                                    <Condition Binding="{Binding FilterComponents.Count}" Value="1" />
                                                                                                    <Condition Binding="{Binding FilterComponents[0].HasMultipleValues}" Value="True" />
                                                                                                </MultiDataTrigger.Conditions>
                                                                                                <Setter Property="Visibility" Value="Visible" />
                                                                                            </MultiDataTrigger>
                                                                                        </Style.Triggers>
                                                                                    </Style>
                                                                                </ItemsControl.Style>
                                                                                <ItemsControl.ItemsPanel>
                                                                                    <ItemsPanelTemplate>
                                                                                        <StackPanel Orientation="Horizontal" />
                                                                                    </ItemsPanelTemplate>
                                                                                </ItemsControl.ItemsPanel>
                                                                                <ItemsControl.ItemTemplate>
                                                                                    <DataTemplate>
                                                                                        <Border Margin="0,0,2,0"
                                                                                                Padding="4,2"
                                                                                                CornerRadius="2"
                                                                                                Background="#bcd7e7">
                                                                                            <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                                                                                        </Border>
                                                                                    </DataTemplate>
                                                                                </ItemsControl.ItemTemplate>
                                                                            </ItemsControl>

                                                                            <!--  Fallback Single Primary Value  -->
                                                                            <Border Margin="0,0,2,0"
                                                                                    Padding="4,2"
                                                                                    CornerRadius="2">
                                                                                <Border.Style>
                                                                                    <Style TargetType="Border">
                                                                                        <Setter Property="Background" Value="LightBlue" />
                                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                                        <Style.Triggers>
                                                                                            <DataTrigger Binding="{Binding IsDateInterval}" Value="True">
                                                                                                <Setter Property="Background" Value="LightGreen" />
                                                                                            </DataTrigger>
                                                                                            <DataTrigger Binding="{Binding HasNoInputValues}" Value="True">
                                                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                                            </DataTrigger>
                                                                                            <MultiDataTrigger>
                                                                                                <MultiDataTrigger.Conditions>
                                                                                                    <Condition Binding="{Binding FilterComponents.Count}" Value="1" />
                                                                                                    <Condition Binding="{Binding FilterComponents[0].HasMultipleValues}" Value="True" />
                                                                                                </MultiDataTrigger.Conditions>
                                                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                                            </MultiDataTrigger>
                                                                                        </Style.Triggers>
                                                                                    </Style>
                                                                                </Border.Style>
                                                                                <TextBlock Text="{Binding PrimaryValue}" VerticalAlignment="Center" />
                                                                            </Border>
                                                                        </Grid>

                                                                        <!--  Fallback Value Operator  -->
                                                                        <Border Background="Transparent"
                                                                                Visibility="{Binding ValueOperatorText, Converter={StaticResource StringToVisibilityConverter}}"
                                                                                Margin="0,0,2,0"
                                                                                Padding="4,2"
                                                                                CornerRadius="2">
                                                                            <TextBlock Text="{Binding ValueOperatorText}" VerticalAlignment="Center" />
                                                                        </Border>

                                                                        <!--  Fallback Secondary Value Component  -->
                                                                        <Border Visibility="{Binding SecondaryValue, Converter={StaticResource StringToVisibilityConverter}}"
                                                                                Margin="0,0,2,0"
                                                                                Padding="4,2"
                                                                                CornerRadius="2">
                                                                            <Border.Style>
                                                                                <Style TargetType="Border">
                                                                                    <Setter Property="Background" Value="LightBlue" />
                                                                                    <Style.Triggers>
                                                                                        <DataTrigger Binding="{Binding IsDateInterval}" Value="True">
                                                                                            <Setter Property="Background" Value="LightGreen" />
                                                                                        </DataTrigger>
                                                                                    </Style.Triggers>
                                                                                </Style>
                                                                            </Border.Style>
                                                                            <TextBlock Text="{Binding SecondaryValue}" VerticalAlignment="Center" />
                                                                        </Border>

                                                                    </StackPanel>
                                                                </Grid>

                                                            </StackPanel>
                                                        </Border>

                                                        <!--  Close Button  -->
                                                        <Button Grid.Column="1"
                                                                Cursor="Hand"
                                                                Command="{Binding RemoveFilterCommand, RelativeSource={RelativeSource AncestorType=sdg:FilterPanel}}"
                                                                CommandParameter="{Binding}"
                                                                HorizontalAlignment="Right"
                                                                VerticalAlignment="Top"
                                                                Width="12"
                                                                Height="12"
                                                                FontFamily="Segoe MDL2 Assets"
                                                                FontSize="8"
                                                                Content="&#xE8BB;"
                                                                ToolTip="Remove this filter">
                                                            <Button.Style>
                                                                <Style BasedOn="{StaticResource TransparentFontIcon_Button_Style}" TargetType="Button">
                                                                    <Setter Property="Visibility" Value="Hidden" />
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Foreground" Value="#CC0000" />
                                                                        </Trigger>
                                                                        <Trigger Property="IsPressed" Value="True">
                                                                            <Setter Property="Foreground" Value="#990000" />
                                                                        </Trigger>
                                                                        <Trigger Property="IsEnabled" Value="False">
                                                                            <Setter Property="Opacity" Value="0.5" />
                                                                            <Setter Property="Cursor" Value="Arrow" />
                                                                        </Trigger>
                                                                        <!--  Trigger visibility when ancestor Grid is hovered  -->
                                                                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Grid}}" Value="True">
                                                                            <Setter Property="Visibility" Value="Visible" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Button.Style>
                                                        </Button>
                                                    </Grid>
                                                </StackPanel>

                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                </WrapPanel>
                            </ScrollViewer>

                            <!--  Action Buttons  -->
                            <StackPanel Grid.Column="2"
                                        Orientation="Horizontal"
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center">

                                <Button x:Name="PART_EditButton"
                                        Command="{Binding EditFiltersCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                        Margin="0,0,8,0"
                                        Width="32"
                                        Height="32"
                                        Content="&#xE70F;"
                                        FontSize="14"
                                        ToolTip="Open advanced filter editor">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource TransparentFontIcon_Button_Style}" TargetType="{x:Type Button}">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#ebeced" />
                                                    <Setter Property="Foreground" Value="#1c1c1c" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#d4d5d6" />
                                                    <Setter Property="Foreground" Value="#000000" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <Button x:Name="PART_ClearAllButton"
                                        Command="{Binding ClearAllFiltersCommand, RelativeSource={RelativeSource TemplatedParent}}"
                                        Width="32"
                                        Height="32"
                                        Content="&#xE894;"
                                        FontSize="14"
                                        ToolTip="Clear all filters">
                                    <Button.Style>
                                        <Style BasedOn="{StaticResource TransparentFontIcon_Button_Style}" TargetType="{x:Type Button}">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#ebeced" />
                                                    <Setter Property="Foreground" Value="#CC0000" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#d4d5d6" />
                                                    <Setter Property="Foreground" Value="#990000" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Default_FilterPanel_Style}" TargetType="{x:Type sdg:FilterPanel}" />

</ResourceDictionary>